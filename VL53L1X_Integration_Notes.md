# VL53L1X集成说明

## 修改内容概述

已成功将HC_SR04超声波测距模块替换为VL53L1X激光测距传感器。

## 硬件连接

VL53L1X与STM32的连接:
- **SDA** → PB4
- **SCL** → PB5  
- **VDD** → 2.6V-3.5V (推荐3.3V)
- **GND** → GND

## 新增文件

### 1. `Driver/VL53L1x_STM32.h`
VL53L1X的接口头文件，定义了主要的API函数。

### 2. `Driver/VL53L1x_STM32.c`
VL53L1X的驱动实现文件，包含:
- 软件I2C实现(使用PB4/PB5)
- 传感器初始化
- 距离读取函数

## 主要函数

```c
// I2C初始化
void VL53L1X_I2C_Init(void);

// 传感器初始化 (返回0表示成功)
uint8_t VL53L1X_Init(void);

// 获取距离 (单位:mm, 返回0xFFFF表示错误)
uint16_t VL53L1X_GetDistance(void);

// 检查数据是否准备好
uint8_t VL53L1X_DataReady(void);
```

## main.c中的修改

1. **头文件替换:**
   - 移除: `#include "HC_SR04.h"`
   - 添加: `#include "VL53L1x_STM32.h"`

2. **初始化代码替换:**
   - 移除: `HC_SR04_IO_Init();`
   - 添加: 
     ```c
     VL53L1X_I2C_Init();
     if(VL53L1X_Init() != 0) {
         OLED_ShowStr(0, 2, "VL53L1X Error!", 2);
         delay_ms(2000);
     }
     ```

3. **距离获取函数修改:**
   - `Get_Distance()`函数中使用`VL53L1X_GetDistance()`替换`Get_SR04_Distance()`
   - VL53L1X返回的距离单位是mm(毫米)
   - 为兼容原有逻辑，代码中将mm乘以10

## 性能特点

### VL53L1X优势
- **更高精度**: ±25mm (典型值)
- **更远距离**: 最远可达4米
- **更快速度**: 最快20ms测量周期
- **抗干扰**: 不受环境噪声影响
- **小角度**: 27°视场角
- **低功耗**: 工作电流约19mA

### 与HC_SR04对比
| 特性 | HC_SR04 | VL53L1X |
|------|---------|---------|
| 测量距离 | 2-400cm | 0-400cm |
| 精度 | ±3mm | ±25mm |
| 视场角 | 15° | 27° |
| 响应时间 | 38ms | 20-100ms |
| 抗干扰 | 中等 | 优秀 |

## 注意事项

1. **I2C地址**: VL53L1X的默认I2C地址是0x52 (8位格式)或0x29 (7位格式)

2. **测量范围**: 
   - 短距离模式: 0-1.3m
   - 中距离模式: 0-3m  
   - 长距离模式: 0-4m

3. **初始化时间**: 传感器上电后需要约10ms启动时间

4. **错误处理**: 如果`VL53L1X_GetDistance()`返回0xFFFF，表示测量失败

5. **I2C频率**: 当前实现约为100kHz，可以通过调整`VL53L1X_I2C_Delay()`中的延时来修改

## 调试建议

如果遇到问题，请检查:

1. ✓ 硬件连接是否正确(特别是SDA和SCL)
2. ✓ 供电电压是否在2.6V-3.5V范围内
3. ✓ I2C上拉电阻是否已连接(通常需要4.7kΩ)
4. ✓ OLED显示是否显示"VL53L1X Error!"
5. ✓ 使用示波器检查I2C信号是否正常

## 编译配置

确保在工程配置中添加了以下文件:
- `Driver/VL53L1x_STM32.c`

并且头文件搜索路径包含:
- `Driver/`

## 未来改进建议

1. 可添加更完整的VL53L1X API支持(ROI设置、校准等)
2. 可改用硬件I2C提高可靠性
3. 可添加中断支持以降低功耗
4. 可实现多传感器支持(通过修改I2C地址)

---
更新日期: 2026年3月27日
